{
    "name": "@test/is-even",
    "version": "0.2.1",
    "main": "index.js",
    "files": [
        "lib",
        "index.d.ts"
    ],
    "types": "index.d.ts",
    "devDependencies": {},
    "scripts": {
        "build": "tsc && cp-cli package.json ./dist/package.json && cp-cli .npmrc ./dist/.npmrc",
        "my-publish": "cd dist && pnpm publish --no-git-checks",
        "test-publish": "cd dist && pnpm publish --no-git-checks --dry-run"
    },
    "publishConfig": {
        "@test:registry": "https://gitlab.dorabot.com/api/v4/projects/4649/packages/npm/"
    }
}